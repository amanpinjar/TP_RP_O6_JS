function greet (name){
    console.log(`Hello, ${name}`)
  }
  greet("aman")
  
  
  // Function  calculate the area of a rectangle
  function calcArea(w, h) {
    let area = w * h; 
    console.log(`the Area of Rectangle is :- ${area}inch`); 
  }
  calcArea(10, 14); 
  
  // Function to convert Fahrenheit to Celsius
  function fahrenheitToCelsius(fahrenheit) {
    var celsius = (fahrenheit - 32) * 5/9;
    return celsius;
}
fahrenheitToCelsius(45); 
  
  // Function to convert Celsius to Fahrenheit
  function celsiusToFahrenheit(celsius) {
    var fahrenheit = (celsius * 9/5) + 32;
    return fahrenheit;
}
celsiusToFahrenheit(34); 
  

  
  
  function add(a, b) {
    return a + b;
  }
  console.log(add(7, 6));
  
 
  function subtract(a, b) {
    return a - b;
  }
  console.log(subtract(7, 6));
  
  
  function multiply(a, b) {
    return a * b;
  }
  console.log(multiply(7, 6));
  
  
  function divide(a, b) {
    return a / b;
  }
  console.log(divide(7, 6));
  


